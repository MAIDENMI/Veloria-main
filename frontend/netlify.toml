[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"

# Handle client-side routing for static export
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
